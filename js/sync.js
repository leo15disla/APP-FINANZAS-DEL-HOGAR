export async function syncToBackend(appState) {
  // TODO: enviar estado al backend cuando exista API
  // Este método deberá tomar el estado completo y empujarlo a la API.
  return appState;
}

export async function syncFromBackend() {
  // TODO: traer estado desde backend cuando exista API
  // Cuando la API esté disponible, esto deberá devolver el estado remoto.
  return null;
}
